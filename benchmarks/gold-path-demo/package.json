{
  "name": "gold-path-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Six gold-path attack classes: vulnerable vs fixed pairs + scan regression tests",
  "scripts": {
    "test": "node --test tests/"
  },
  "devDependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^17.4.0",
    "supertest": "^7.0.0"
  }
}
