# AI security review prompt (manual / Claude Code)

**Scope:** `vulnerable/` only.

**Ask:** Whether `/admin` (or similar) routes are protected by authentication/authorization middleware; cite missing guards; propose middleware ordering or role checks.

**Save to** `results/<date>/03-admin-auth/ai.json`.
