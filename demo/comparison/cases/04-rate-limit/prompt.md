# AI security review prompt (manual / Claude Code)

**Scope:** `vulnerable/` only.

**Ask:** Whether login or abuse-sensitive routes use rate limiting or equivalent; suggest `express-rate-limit` or edge/WAF patterns.

**Save to** `results/<date>/04-rate-limit/ai.json`.
