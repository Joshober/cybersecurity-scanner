# AI security review prompt (manual / Claude Code)

**Scope:** `vulnerable/` only.

**Ask:** Whether JWT signing uses a hard-coded or weak secret; recommend env-based secrets and key rotation.

**Save to** `results/<date>/06-weak-jwt/ai.json`.
