# VibeScan (harness)

Runs **in-process** from `vibescan/dist` via `scanProjectAsync` (same options as the interactive demo: `crypto` + `injection`, severity threshold `info`).

Build before running:

```bash
npm run build -w vibescan
```
