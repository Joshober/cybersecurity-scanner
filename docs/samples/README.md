# Sample artifacts

| File | Purpose |
|------|---------|
| [`policy.sample.json`](./policy.sample.json) | Example **secure-arch policy** JSON for the VibeScan ↔ secure-arch PoC bridge (`vibescan/scripts/policy-eval.mjs`). |

From repo root:

```bash
node vibescan/scripts/policy-eval.mjs docs/samples/policy.sample.json vibescan-out.json
```
